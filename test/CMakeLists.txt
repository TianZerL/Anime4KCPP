if (AC_TEST_UTIL)
    add_subdirectory(util)
endif()
if (AC_TEST_VIDEO)
    add_subdirectory(video)
endif()
if (AC_TEST_WASM)
    if(NOT EMSCRIPTEN)
        message(FATAL_ERROR "Build wasm test need emscripten toolchain")
    endif()
    add_subdirectory(wasm)
endif()
